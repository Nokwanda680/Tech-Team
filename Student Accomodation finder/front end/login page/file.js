/*

const images=document.querySelector('.scroll-img');
    const observer= new IntersectionObserver((entries)=>{
        entries.forEach(entry=>{
            if (entry.isIntersecting){
                entry.target.classList.add('appear');
            }
        });
    },{threshold: 0.2});
    Image.forEach(img=>{
        observer.observe("advert 1.jpg")
    });*/
    